# v1.1

## Highlights

- Set the production Android package to `com.jeiel.daddygifttracker`.
- Added a new app icon, GitHub Pages landing image, favicon, README hero, and Play Console graphics.
- Added local release signing support under `.keystore/` and a Desktop export task for Play handoff.
- Removed example tests and replaced them with app identity/package checks.

## Build

```powershell
.\gradlew.bat :app:testDebugUnitTest :app:bundleRelease :app:exportReleaseToDesktop
```
